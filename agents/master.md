# Studio Master Agent — Compatibility Entry

本文件仅保留兼容入口。该角色的长期配置唯一事实源在 `agents/master/`。

按顺序读取：`ROLE.md` → `CAPABILITIES.md` → `CONSTRAINTS.md` → `DECISIONS.md` → `SKILLS.md`。

全局治理同时遵循根目录 `AGENTS.md`、`rules/` 与 `governance/`。
